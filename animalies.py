""" Задание от 13.07.2026
Детектор аномалий в сигнале
Написать функции для поиска аномалий в потоке данных с датчиков.
Написать функцию first_unique(signals) — принимает список целых чисел (сигналов),
возвращает первый элемент который встречается ровно один раз. Если таких нет — вернуть -1.
- подсказка от папы: в списке есть методы подсчета частоты элемента (count)

Написать функцию find_pair(signals, target) — возвращает первую пару индексов (i, j)
где i < j и signals[i] + signals[j] == target.
Если пары нет — вернуть None. Использовать множество для эффективного поиска.
- подсказка от папы: проходим фором, смотрим условие выполняется или нет.

Написать функцию sliding_anomaly(signals, window, threshold) — скользящее окно размером window.
Для каждого окна считает среднее. Если какое-то значение в окне отличается от среднего больше чем на threshold — это аномалия.
Возвращает список индексов аномальных значений.
Что использовать:
Словарь — для подсчёта вхождений в first_unique
Множество — для хранения просмотренных значений в find_pair
for i in range(len(signals) - window + 1) — скользящее окно
abs() — модуль разности
Пример работы:
signals = [4, 3, 2, 7, 8, 2, 3, 1]

first_unique(signals)         → 4  (первая уникальная)
find_pair(signals, 10)        → (1, 3)  # 3+7=10, индексы 1 и 3
sliding_anomaly([1,1,1,10,1,1], window=3, threshold=3) → [3]
"""

def first_unique(signals):
	pass

def find_pair(signals, threshold):
	pass

def sliding_anomaly(signals, window, threshold):
	pass
